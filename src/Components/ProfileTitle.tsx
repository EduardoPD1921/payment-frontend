import React from 'react';

import {
    ProfileTitleSection
} from '../StyledComponents';

const ProfileTitle: React.FC = () => {
    return (
        <ProfileTitleSection>
            test
        </ProfileTitleSection>
    )
}

export default ProfileTitle;